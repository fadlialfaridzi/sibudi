<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - SIBUDI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <script
      defer
      src="https://cdn.jsdelivr.net/npm/preline@latest/dist/preline.min.js"
    ></script>
  </head>
  <body
    class="min-h-screen bg-gradient-to-r from-green-100 via-green-300 to-green-700"
  >
    <div class="min-h-screen flex items-center justify-center py-8">
      <div class="w-full max-w-6xl mx-auto px-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-stretch">
          <!-- Card kiri -->
          <div
            class="bg-white/70 backdrop-blur-md rounded-[28px] shadow-2xl p-10 md:p-12 h-[560px] md:h-[640px] flex flex-col"
          >
            <h1
              class="text-6xl md:text-7xl font-extrabold text-green-800 tracking-widest mb-10"
            >
              SIBUDI
            </h1>

            <form action="/login" method="post" class="space-y-6">
              <div>
                <label class="sr-only" for="email">Email</label>
                <input
                  id="email"
                  name="email"
                  type="email"
                  placeholder="Email"
                  class="w-full rounded-full border border-gray-200 bg-white/95 px-6 py-4 text-base shadow focus:outline-none focus:ring-2 focus:ring-green-600/30"
                  required
                />
              </div>

              <div>
                <div class="relative">
                  <label class="sr-only" for="password">Password</label>
                  <input
                    id="password"
                    name="password"
                    type="password"
                    placeholder="Password"
                    class="w-full rounded-full border border-gray-200 bg-white/95 px-6 py-4 pr-12 text-base shadow focus:outline-none focus:ring-2 focus:ring-green-600/30"
                    required
                  />
                  <button
                    type="button"
                    class="absolute inset-y-0 right-4 my-auto text-gray-500"
                    onclick="const i=document.getElementById('password'); i.type = i.type==='password' ? 'text' : 'password'"
                  >
                    <i class="fa-regular fa-eye"></i>
                  </button>
                </div>
                <div class="mt-2 text-right">
                  <a href="#" class="text-sm text-gray-500 hover:text-green-700"
                    >Lupa Kata Sandi?</a
                  >
                </div>
              </div>

              <button
                type="submit"
                class="w-full rounded-full border-2 border-indigo-900/80 text-indigo-900 font-semibold tracking-wide py-3.5 hover:bg-indigo-900/5 transition"
              >
                LOGIN
              </button>
            </form>
          </div>

          <!-- Area kanan: gambar -->
          <div
            class="hidden md:block rounded-[28px] overflow-hidden shadow-2xl h-[560px] md:h-[640px]"
          >
            <img
              src="/images/Perpustakaan Unand.jpg"
              alt="Universitas Andalas"
              class="w-full h-full object-cover"
            />
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
