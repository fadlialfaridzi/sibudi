<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/output.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: 'Inter', sans-serif;
        background-color: #f9fafb;
      }
    </style>
  </head>

  <body class="min-h-screen flex flex-col">
    <%- include('../partials/navbarOutside', { title: 'Edit Profil - SiBuDi' }) %>

    <main class="flex-grow pt-24 pb-16 bg-gray-50">
      <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6">
          <h1 class="text-2xl font-bold text-[#2F7A33] mb-6">
            Edit Profil Member
          </h1>

          <form action="/outside/updateProfile" method="POST" class="space-y-6">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1"
                >Nama Lengkap</label
              >
              <input
                type="text"
                name="member_name"
                value="<%= member.member_name || '' %>"
                class="block w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-[#FFD700]"
                required
              />
            </div>

            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1"
                >Jenis Kelamin</label
              >
              <select
                name="gender_text"
                class="block w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-[#FFD700]"
              >
                <option value="">Pilih</option>
                <option value="Laki-laki" <%= member.gender === 1 ? 'selected' : '' %>>
                  Laki-laki
                </option>
                <option value="Perempuan" <%= member.gender === 2 ? 'selected' : '' %>>
                  Perempuan
                </option>
              </select>
            </div>

            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1"
                >Nomor Telepon</label
              >
              <input
                type="text"
                name="member_phone"
                value="<%= member.member_phone || '' %>"
                class="block w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-[#FFD700]"
              />
            </div>

            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1"
                >Email</label
              >
              <input
                type="email"
                name="member_email"
                value="<%= member.member_email || '' %>"
                class="block w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-[#FFD700]"
              />
            </div>

            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1"
                >Alamat</label
              >
              <textarea
                name="member_address"
                rows="3"
                class="block w-full border border-gray-300 rounded-lg px-3 py-2 focus:ring-2 focus:ring-[#FFD700]"
              ><%= member.member_address || '' %></textarea>
            </div>

            <div class="flex justify-end gap-3">
              <a
                href="/outside/profile"
                class="px-4 py-2 rounded-lg bg-gray-200 hover:bg-gray-300 text-gray-800 font-medium"
                >Batal</a
              >
              <button
                type="submit"
                class="px-4 py-2 rounded-lg bg-[#2F7A33] text-white font-medium hover:bg-[#256528] focus:ring-2 focus:ring-[#FFD700]"
              >
                Simpan Perubahan
              </button>
            </div>
          </form>
        </div>
      </div>
    </main>

    <%- include('../partials/footer-simple') %>
  </body>
</html>
