<!DOCTYPE html>
<html lang="id">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title %> - SiBuDi</title>
        <script src="https://cdn.tailwindcss.com"></script>
        <script src="https://unpkg.com/preline/dist/preline.js"></script>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />
        <link rel="stylesheet" href="/css/profile-style.css" />
    </head>

    <body class="min-h-screen flex flex-col">
        <%- include('../partials/navbarOutside', { title: 'Ganti Password - SiBuDi' }) %>

        <main class="flex-grow pt-24 pb-16">
            <div class="max-w-lg mx-auto px-4 sm:px-6 lg:px-8">
                <!-- Card -->
                <div class="relative bg-white border border-green-100 rounded-2xl shadow-lg hover:shadow-2xl transition-all duration-300 p-8 overflow-hidden">
                    <!-- Decorative glow -->
                    <div class="absolute -top-20 -right-20 w-40 h-40 bg-yellow-100 rounded-full blur-3xl opacity-40"></div>
                    <div class="absolute -bottom-20 -left-20 w-40 h-40 bg-green-100 rounded-full blur-3xl opacity-40"></div>

                    <div class="relative text-center mb-8">
                        <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-gradient-to-br from-green-100 to-yellow-100 shadow-inner mb-4">
                            <i class="fa-solid fa-key text-[#2F7A33] text-2xl"></i>
                        </div>
                        <h1 class="text-3xl font-bold text-[#2F7A33]">Ganti Password</h1>
                        <p class="text-gray-600 text-sm mt-1">Perbarui kata sandi Anda untuk keamanan yang lebih baik 🔒</p>
                    </div>

                    <!-- Notifikasi -->
                    <% if (error) { %>
                    <div class="mb-4 bg-red-50 border border-red-200 text-red-700 text-sm px-4 py-3 rounded-lg animate-pulse"><i class="fa-solid fa-circle-exclamation mr-2"></i><%= error %></div>
                    <% } %> <% if (success) { %>
                    <div class="mb-4 bg-green-50 border border-green-200 text-green-700 text-sm px-4 py-3 rounded-lg animate-bounce"><i class="fa-solid fa-circle-check mr-2"></i><%= success %></div>
                    <% } %>

                    <!-- Form -->
                    <form id="changePasswordForm" action="/outside/changePassword" method="POST" class="space-y-6">
                        <div class="group">
                            <label class="block text-sm font-medium text-gray-700 mb-1" for="oldPassword">Password Lama</label>
                            <div class="relative">
                                <input type="password" id="oldPassword" name="oldPassword" required class="peer w-full border border-gray-200 rounded-lg px-3 py-2 pr-12 focus:ring-2 focus:ring-[#FFD700] focus:border-transparent transition-all" />
                                <button type="button" data-toggle="oldPassword" class="absolute inset-y-0 right-0 flex items-center px-4 text-gray-400 hover:text-[#2F7A33]">
                                    <i class="fa-solid fa-eye"></i>
                                </button>
                            </div>
                        </div>

                        <div class="group">
                            <label class="block text-sm font-medium text-gray-700 mb-1" for="newPassword">Password Baru</label>
                            <div class="relative">
                                <input
                                    type="password"
                                    id="newPassword"
                                    name="newPassword"
                                    required
                                    minlength="6"
                                    class="peer w-full border border-gray-200 rounded-lg px-3 py-2 pr-12 focus:ring-2 focus:ring-[#FFD700] focus:border-transparent transition-all" />
                                <button type="button" data-toggle="newPassword" class="absolute inset-y-0 right-0 flex items-center px-4 text-gray-400 hover:text-[#2F7A33]">
                                    <i class="fa-solid fa-eye"></i>
                                </button>
                            </div>
                        </div>

                        <div class="group">
                            <label class="block text-sm font-medium text-gray-700 mb-1" for="confirmPassword">Konfirmasi Password Baru</label>
                            <div class="relative">
                                <input
                                    type="password"
                                    id="confirmPassword"
                                    name="confirmPassword"
                                    required
                                    minlength="6"
                                    class="peer w-full border border-gray-200 rounded-lg px-3 py-2 pr-12 focus:ring-2 focus:ring-[#FFD700] focus:border-transparent transition-all" />
                                <button type="button" data-toggle="confirmPassword" class="absolute inset-y-0 right-0 flex items-center px-4 text-gray-400 hover:text-[#2F7A33]">
                                    <i class="fa-solid fa-eye"></i>
                                </button>
                            </div>
                        </div>

                        <button
                            type="submit"
                            class="w-full bg-[#2F7A33] hover:bg-[#256928] active:scale-[0.98] text-white font-semibold py-3 rounded-lg shadow-md hover:shadow-lg transition-all duration-300 focus:ring-2 focus:ring-[#FFD700]/40">
                            <i class="fa-solid fa-save mr-2"></i> Simpan Password Baru
                        </button>

                        <a href="/outside/profile" class="block text-center mt-4 text-sm text-[#2F7A33] hover:text-[#256928] hover:underline transition-colors duration-200"> <i class="fa-solid fa-arrow-left mr-1"></i> Kembali ke Profil </a>
                    </form>

                    <!-- Modal Konfirmasi -->
                    <div id="confirmModal" class="fixed inset-0 bg-black/40 backdrop-blur-sm hidden items-center justify-center z-50 transition-opacity duration-300">
                        <div class="bg-white relative rounded-2xl shadow-2xl border border-green-100 w-full max-w-md p-6 transform scale-95 opacity-0 transition-all duration-300">
                            <div class="text-center">
                                <div class="inline-flex items-center justify-center w-14 h-14 rounded-full bg-gradient-to-br from-green-100 to-yellow-100 shadow-inner mb-3">
                                    <i class="fa-solid fa-question text-[#2F7A33] text-2xl"></i>
                                </div>
                                <h2 class="text-xl font-bold text-[#2F7A33] mb-1">Yakin ganti password?</h2>
                                <p class="text-gray-600 text-sm">Pastikan password baru sudah benar dan aman 🔒</p>
                            </div>

                            <div class="mt-6 flex justify-center gap-3">
                                <button id="cancelBtn" type="button" class="px-4 py-2 rounded-lg border border-gray-200 text-gray-600 hover:bg-gray-50 transition-all duration-200">Batal</button>
                                <button id="confirmBtn" type="button" class="px-4 py-2 rounded-lg bg-[#2F7A33] hover:bg-[#256928] text-white font-semibold shadow-md hover:shadow-lg transition-all duration-200">Ya, Ganti</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>

        <%- include('../partials/footer-simple') %>

        <script>
            const form = document.getElementById('changePasswordForm');
            const modal = document.getElementById('confirmModal');
            const confirmBtn = document.getElementById('confirmBtn');
            const cancelBtn = document.getElementById('cancelBtn');

            form.addEventListener('submit', function (e) {
                e.preventDefault();
                modal.classList.remove('hidden');
                setTimeout(() => {
                    modal.classList.add('flex');
                    const box = modal.querySelector('div');
                    box.classList.remove('scale-95', 'opacity-0');
                    box.classList.add('scale-100', 'opacity-100');
                }, 10);
            });

            cancelBtn.addEventListener('click', () => {
                const box = modal.querySelector('div');
                box.classList.add('scale-95', 'opacity-0');
                setTimeout(() => {
                    modal.classList.add('hidden');
                    modal.classList.remove('flex');
                }, 200);
            });

            confirmBtn.addEventListener('click', () => {
                form.submit();
            });

            const togglePasswordButtons = document.querySelectorAll('[data-toggle]');
            togglePasswordButtons.forEach(button => {
                button.addEventListener('click', () => {
                    const targetId = button.dataset.toggle;
                    const targetInput = document.getElementById(targetId);
                    const icon = button.querySelector('i');

                    if (targetInput.type === 'password') {
                        targetInput.type = 'text';
                        icon.classList.remove('fa-eye');
                        icon.classList.add('fa-eye-slash');
                    } else {
                        targetInput.type = 'password';
                        icon.classList.remove('fa-eye-slash');
                        icon.classList.add('fa-eye');
                    }
                });
            });
        </script>
    </body>
</html>